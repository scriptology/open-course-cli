#!/bin/sh
# Mirrors the CI "Format" job so formatting failures are caught before pushing.
set -e

if ! command -v cargo >/dev/null 2>&1; then
    echo "pre-commit: cargo not found, skipping fmt check" >&2
    exit 0
fi

cargo fmt --all -- --check
